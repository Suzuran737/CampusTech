# 复制为 .env 并填写实际值
# Copy-Item .env.example .env

DATABASE_URL="postgresql://postgres:password@localhost:5432/campustech?schema=public"
JWT_SECRET="your-jwt-secret-change-in-production"

# RSS 定时抓取（每 6 小时）；设为 false 可关闭
RSS_CRON_ENABLED=true

PORT=3000
